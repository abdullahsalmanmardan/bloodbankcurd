import Form from "../../components/shared/Form/Form";

const Login = () => {

  return (
    <div>
      <Form submitButton="Login" formTitle={"Login Form"} formType={"login"} />
    </div>
  );
};

export default Login;
